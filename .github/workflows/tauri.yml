name: "Tauri Desktop — Build & Release"

on:
  push:
    branches: [main, master]
    tags: ["v*"]
    paths:
      - "frontend/**"
      - "backend/**"
      - "src-tauri/**"
      - ".github/workflows/tauri.yml"
      - "package.json"
      - "scripts/**"
  pull_request:
    branches: [main, master]
    paths:
      - "frontend/**"
      - "backend/**"
      - "src-tauri/**"
  workflow_dispatch:

concurrency:
  group: tauri-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: windows-latest
            args: ""
            target: x86_64-pc-windows-msvc
            binary_suffix: ".exe"
            bundle_glob: "src-tauri/target/release/bundle/**/*.{exe,msi}"
          - platform: ubuntu-22.04
            args: ""
            target: x86_64-unknown-linux-gnu
            binary_suffix: ""
            bundle_glob: "src-tauri/target/release/bundle/**/*.{deb,AppImage,rpm}"
          - platform: macos-latest
            args: "--target universal-apple-darwin"
            target: universal-apple-darwin
            binary_suffix: ""
            bundle_glob: "src-tauri/target/universal-apple-darwin/release/bundle/**/*.{dmg,app.tar.gz}"

    runs-on: ${{ matrix.platform }}
    timeout-minutes: 60
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: |
            frontend/package-lock.json
            package-lock.json

      - name: Setup Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target == 'universal-apple-darwin' && 'aarch64-apple-darwin,x86_64-apple-darwin' || matrix.target }}

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            backend -> target
            src-tauri -> target
          # keep cache lean; sidecar is heavy
          cache-on-failure: false

      - name: Install Linux deps (WebKit, AppIndicator, FFmpeg headers)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            librsvg2-dev \
            patchelf \
            libssl-dev \
            libayatana-appindicator3-dev \
            xdg-utils \
            libfuse2 \
            ffmpeg

      - name: Install macOS deps (vosk)
        if: matrix.platform == 'macos-latest'
        run: |
          brew update
          brew install vosk-api || brew install vosk || echo "vosk brew not found - will try fallback"
          # Ensure libvosk is findable for linker; vosk crate looks at VOSK_LIB_DIR or pkg-config
          ls -R /opt/homebrew/opt/vosk* 2>/dev/null | head -n 50 || true
          ls -R /usr/local/opt/vosk* 2>/dev/null | head -n 50 || true

      - name: Install frontend deps
        run: npm ci
        working-directory: frontend

      - name: Install root deps (Tauri CLI)
        # package.json at root pins tauri CLI version used locally and in CI
        run: |
          if [ -f package-lock.json ]; then npm ci; else npm install; fi
        shell: bash

      - name: Build backend sidecar binary
        shell: bash
        env:
          SQLX_OFFLINE: true
        run: |
          set -euo pipefail
          echo "Building novaclip-api for ${{ matrix.target }} ..."
          if [[ "${{ matrix.target }}" == "universal-apple-darwin" ]]; then
            # macOS: build without vosk (no libvosk on runner), keep whisper
            cargo build --release --target aarch64-apple-darwin -p novaclip-api --manifest-path backend/Cargo.toml --features whisper
            cargo build --release --target x86_64-apple-darwin -p novaclip-api --manifest-path backend/Cargo.toml --features whisper
            # Create fat binary via lipo for single sidecar artifact
            mkdir -p src-tauri/binaries
            lipo -create \
              backend/target/aarch64-apple-darwin/release/novaclip-api \
              backend/target/x86_64-apple-darwin/release/novaclip-api \
              -output src-tauri/binaries/novaclip-api-universal-apple-darwin
            # Also produce arch-specific copies expected by Tauri externalBin resolution
            cp src-tauri/binaries/novaclip-api-universal-apple-darwin src-tauri/binaries/novaclip-api-aarch64-apple-darwin
            cp src-tauri/binaries/novaclip-api-universal-apple-darwin src-tauri/binaries/novaclip-api-x86_64-apple-darwin
            ls -lh src-tauri/binaries/
          elif [[ "${{ matrix.platform }}" == "windows-latest" ]]; then
            cargo build --release --target ${{ matrix.target }} -p novaclip-api --manifest-path backend/Cargo.toml --features full
            mkdir -p src-tauri/binaries
            cp backend/target/${{ matrix.target }}/release/novaclip-api.exe src-tauri/binaries/novaclip-api-${{ matrix.target }}.exe
            # Also copy without suffix for Tauri dev fallback
            cp backend/target/${{ matrix.target }}/release/novaclip-api.exe src-tauri/binaries/novaclip-api.exe || true
            ls -lh src-tauri/binaries/
          else
            cargo build --release --target ${{ matrix.target }} -p novaclip-api --manifest-path backend/Cargo.toml --features whisper
            mkdir -p src-tauri/binaries
            cp backend/target/${{ matrix.target }}/release/novaclip-api src-tauri/binaries/novaclip-api-${{ matrix.target }}
            cp backend/target/${{ matrix.target }}/release/novaclip-api src-tauri/binaries/novaclip-api || true
            ls -lh src-tauri/binaries/
          fi

      - name: Bundle portable Python + FFmpeg (zero-dep)
        shell: bash
        run: |
          set -e
          mkdir -p src-tauri/resources/python src-tauri/binaries
          if [[ "${{ matrix.platform }}" == "windows-latest" ]]; then
            echo "Bundling Python embed + ffmpeg for Windows..."
            curl.exe -L -o /tmp/python-embed.zip https://www.python.org/ftp/python/3.11.9/python-3.11.9-embed-amd64.zip || curl -L -o /tmp/python-embed.zip https://www.python.org/ftp/python/3.11.9/python-3.11.9-embed-amd64.zip
            mkdir -p /tmp/py && unzip -q /tmp/python-embed.zip -d src-tauri/resources/python || 7z x /tmp/python-embed.zip -o"src-tauri/resources/python" -y
            curl -L -o src-tauri/resources/python/get-pip.py https://bootstrap.pypa.io/get-pip.py || curl.exe -L -o src-tauri/resources/python/get-pip.py https://bootstrap.pypa.io/get-pip.py
            # ffmpeg win gpl
            curl.exe -L -o /tmp/ffmpeg.zip https://github.com/BtbN/FFmpeg-Builds/releases/download/latest/ffmpeg-master-latest-win64-gpl.zip || curl -L -o /tmp/ffmpeg.zip https://github.com/BtbN/FFmpeg-Builds/releases/download/latest/ffmpeg-master-latest-win64-gpl.zip
            unzip -q /tmp/ffmpeg.zip -d /tmp 2>/dev/null || 7z x /tmp/ffmpeg.zip -o/tmp -y
            cp /tmp/ffmpeg-master-latest-win64-gpl/bin/ffmpeg.exe src-tauri/binaries/ffmpeg-x86_64-pc-windows-msvc.exe 2>/dev/null || cp /tmp/ffmpeg-*/bin/ffmpeg.exe src-tauri/binaries/ffmpeg-x86_64-pc-windows-msvc.exe 2>/dev/null || cp $(which ffmpeg 2>/dev/null) src-tauri/binaries/ffmpeg-x86_64-pc-windows-msvc.exe 2>/dev/null || echo "ffmpeg win copy fallback"
            cp src-tauri/binaries/ffmpeg-x86_64-pc-windows-msvc.exe src-tauri/binaries/ffmpeg.exe 2>/dev/null || true
          elif [[ "${{ matrix.platform }}" == "ubuntu-22.04" ]]; then
            echo "Bundling Python + ffmpeg for Linux..."
            curl -L -o /tmp/python.tar.gz "https://github.com/indygreg/python-build-standalone/releases/download/20240725/cpython-3.11.9+20240725-x86_64-unknown-linux-gnu-install_only.tar.gz"
            mkdir -p src-tauri/resources/python && tar -xzf /tmp/python.tar.gz -C src-tauri/resources/python --strip-components=1
            which ffmpeg && cp $(which ffmpeg) src-tauri/binaries/ffmpeg-x86_64-unknown-linux-gnu && cp src-tauri/binaries/ffmpeg-x86_64-unknown-linux-gnu src-tauri/binaries/ffmpeg || echo "ffmpeg linux copy"
          else
            echo "Bundling Python + ffmpeg for macOS..."
            curl -L -o /tmp/python.tar.gz "https://github.com/indygreg/python-build-standalone/releases/download/20240725/cpython-3.11.9+20240725-aarch64-apple-darwin-install_only.tar.gz" || curl -L -o /tmp/python.tar.gz "https://github.com/indygreg/python-build-standalone/releases/download/20240725/cpython-3.11.9+20240725-x86_64-apple-darwin-install_only.tar.gz"
            mkdir -p src-tauri/resources/python && tar -xzf /tmp/python.tar.gz -C src-tauri/resources/python --strip-components=1 || echo "python mac extract"
            which ffmpeg || brew install ffmpeg; cp $(which ffmpeg) src-tauri/binaries/ffmpeg-universal-apple-darwin 2>/dev/null || cp $(which ffmpeg) src-tauri/binaries/ffmpeg 2>/dev/null || true
            cp src-tauri/binaries/ffmpeg-universal-apple-darwin src-tauri/binaries/ffmpeg-aarch64-apple-darwin 2>/dev/null || true
            cp src-tauri/binaries/ffmpeg-universal-apple-darwin src-tauri/binaries/ffmpeg-x86_64-apple-darwin 2>/dev/null || true
          fi
          ls -lh src-tauri/resources/python 2>/dev/null | head -20 || true
          ls -lh src-tauri/binaries/ffmpeg* 2>/dev/null | head -20 || true

      - name: Build frontend (Vite)
        env:
          VITE_API_URL: http://127.0.0.1:8000
        run: npm run build
        working-directory: frontend

      - name: Tauri build
        uses: tauri-apps/tauri-action@v0.5
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # To enable auto-updater signing, generate a keypair (`tauri signer generate`) and set:
          # TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          # TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          projectPath: src-tauri
          tagName: v__VERSION__
          releaseName: "NovaClip v__VERSION__"
          releaseBody: "NovaClip desktop build — Windows (NSIS/MSI) · macOS (DMG) · Linux (AppImage/deb/rpm).\n\nSee install notes in README. Bundled backend sidecar runs on 127.0.0.1:8000."
          releaseDraft: true
          prerelease: false
          args: ${{ matrix.args }}
          includeUpdaterJson: false

      - name: Collect bundles for artifact upload
        shell: bash
        run: |
          echo "Bundles:"
          find src-tauri/target -type f \( -name "*.exe" -o -name "*.msi" -o -name "*.deb" -o -name "*.AppImage" -o -name "*.rpm" -o -name "*.dmg" -o -name "*.app.tar.gz" \) | head -n 100 || true
          ls -R src-tauri/target/release/bundle 2>/dev/null | head -n 100 || true
          ls -R src-tauri/target/universal-apple-darwin/release/bundle 2>/dev/null | head -n 100 || true

      - name: Upload bundles
        uses: actions/upload-artifact@v4
        with:
          name: novaclip-${{ matrix.platform }}-${{ matrix.target }}
          path: |
            src-tauri/target/release/bundle/**
            src-tauri/target/universal-apple-darwin/release/bundle/**
          if-no-files-found: warn
          retention-days: 14

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all bundles
        uses: actions/download-artifact@v4
        with:
          path: bundles
          merge-multiple: true

      - name: List bundles
        run: ls -R bundles | head -n 200

      - name: Publish to GitHub Release (re-use draft created by tauri-action)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${GITHUB_REF#refs/tags/}"
          echo "Tag: $TAG"
          # tauri-action already created a draft release; just ensure all files are attached if any missed
          gh release upload "$TAG" bundles/**/* --clobber || echo "gh upload finished (some duplicates may be skipped)"
