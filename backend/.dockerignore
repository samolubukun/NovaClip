# Rust build cache
target/

# Local Python venv (rebuilt inside the image)
novaclip_reframe/venv/

# Runtime data
outputs/
temp/

# Large models not shipped in the image (Windows Vosk runtime is useless on
# Linux; the 148MB Whisper binary is downloaded separately)
models/ggml-base.bin
models/vosk-win64-0.3.45

# Databases / misc
*.db
*.db-journal
*.db-wal

# Git / editor
.git
.gitignore
*.md
